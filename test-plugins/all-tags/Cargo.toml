[package]
name = "test-all-tags-plugin"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
patches-core = { path = "../../patches-core" }
patches-ffi-common = { path = "../../patches-ffi-common" }
