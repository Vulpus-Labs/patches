[package]
name = "patches-io-ring"
version = "0.6.0"
edition = "2021"
publish = false

[lib]
name = "patches_io_ring"
path = "src/lib.rs"

[dependencies]
patches-core = { path = "../patches-core" }
rtrb = "0.3"
