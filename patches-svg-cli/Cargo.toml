[package]
name = "patches-svg-cli"
version = "0.1.0"
edition = "2021"
description = "CLI that renders a .patches file to SVG"
publish = false

[[bin]]
name = "patches-svg"
path = "src/main.rs"

[dependencies]
patches-dsl = { path = "../patches-dsl" }
patches-svg = { path = "../patches-svg" }
