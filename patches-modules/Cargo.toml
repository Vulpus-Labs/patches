[package]
name = "patches-modules"
version = "0.6.0"
edition = "2021"
publish = false

[dependencies]
patches-core = { path = "../patches-core" }
patches-dsp = { path = "../patches-dsp" }
rtrb = "0.3"

[dev-dependencies]
patches-core = { path = "../patches-core", features = ["test-support"] }

[lib]
name = "patches_modules"
path = "src/lib.rs"
doctest = false
