[package]
name = "patches-manifest"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "patches_manifest"
path = "src/lib.rs"

[dependencies]
patches-core = { path = "../patches-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
