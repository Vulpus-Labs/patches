[package]
name = "patches-interpreter"
version = "0.5.0"
edition = "2021"

[lib]
name = "patches_interpreter"
path = "src/lib.rs"
doctest = false

[dependencies]
patches-core = { path = "../patches-core" }
patches-dsl  = { path = "../patches-dsl" }
patches-modules = { path = "../patches-modules" }
