# v0.7.0 docs review report

Date: 2026-04-24
Scope: README.md, RELEASE_NOTES.md, docs/src/* (mdBook), verified against code.

## Summary of issues found

### RELEASE_NOTES.md — stale

Still titled "Patches v0.2". Predates full DSL, hot-reload, CLAP host,
FFI plugins, LSP+VSCode, and ~50 modules added since. Needs full
rewrite for v0.7.0. Preserve macOS quarantine / Windows SmartScreen
workarounds (still accurate).

### README.md — wrong module names + stale inventory

Verified against `patches-modules/src/lib.rs` registry list.

**Wrong module names (examples and table):**

- `PolyMidiIn` → `PolyMidiToCv` (lines 8, 163, 324, 341–342)
- `MidiIn` → `MidiToCv` (line 162)

Examples in `examples/*.patches` already use the correct names; the
README drifted.

**Missing from module table:**

- Drum synth section: `Kick`, `Snare`, `Clap`, `ClosedHiHat`,
  `OpenHiHat`, `Tom`, `Cymbal`, `Claves`, `MidiDrumset`
- MIDI: `MidiArp`, `MidiDelay`, `MidiSplit`, `MidiTranspose`
- Dynamics: `StereoLimiter`
- Effects: `Bitcrusher`, `Drive`, `TransientShaper`
- Sequencers: `MasterSequencer`, `PatternPlayer`
- Utilities: `TempoSync`, `MsTicker`, `TriggerToSync`, `SyncToTrigger`,
  `HostTransport`

**Workspace layout missing crates** (per `Cargo.toml` members):
`patches-registry`, `patches-diagnostics`, `patches-host`,
`patches-cpal`, `patches-planner`, `patches-svg`,
`patches-tracker-core`, `patches-vintage`, `patches-alloc-trap`,
`patches-tools`.

**"Current state" omits v0.7 features:** CLAP plugin host, FFI plugin
system, oversampling, WAV recording, alloc-trap, panic containment.

### docs/src/engine-internals.md — stale periodic-update section

Lines ~145–156 describe the deleted `PeriodicUpdate` trait and
`as_periodic()` raw-pointer caching. E114 / ADR 0052 replaced this
with a `Module::periodic_update` default method gated by
`Module::wants_periodic()`. Rewrite to match.

### docs/src/modules/README.md — category table errors

- Line 10: `StereoVca` does not exist — remove.
- Line 11: `MonoToMono` does not exist — replace with `Mixer` / add
  `PolyMixer`, `StereoPolyMixer`.
- Line 9: Filters missing `Bandpass` / `PolyBandpass` / `Svf` / `PolySvf`.
- Line 16: MIDI row uses `MidiIn`/`PolyMidiIn` (wrong); missing
  `MidiToCv`, `PolyMidiToCv`, `MidiCC`, `MidiArp`, `MidiDelay`, etc.
- Line 17: Delays missing `StereoDelay`, `FdnReverb`, `ConvReverb`,
  `StereoConvReverb`.
- Line 18: Dynamics missing `StereoLimiter`.
- Line 19: Utilities missing `Tuner`, `PolyTuner`, `Sah`, `PolySah`,
  `Quant`, `PolyQuant`, `RingMod`.

### docs/src/modules/*.md detail pages — missing module sections

Grep shows no mention of `MidiArp`, `MidiDelay`, `StereoLimiter`,
`ConvReverb`, `ConvolutionReverb` in any detail page. Add sections.

### docs/src/introduction.md, building-a-patch.md, anatomy-of-a-synth.md, live-coding.md, dsl-reference.md, implementing-modules.md

Reviewed — accurate for v0.7.0, no edits required.

## Implementation plan

1. Rewrite `RELEASE_NOTES.md`.
2. README.md: fix module names, rewrite module table, update workspace
   layout, extend "Current state".
3. `docs/src/engine-internals.md`: rewrite periodic-updates section.
4. `docs/src/modules/README.md`: rewrite category table.
5. `docs/src/modules/midi.md`, `delays.md`, `dynamics.md`: add missing
   module sections.
