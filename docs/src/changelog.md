# Changelog

The authoritative changelog is
[`RELEASE_NOTES.md`](https://github.com/Vulpus-Labs/patches/blob/main/RELEASE_NOTES.md)
at the repository root. It is updated with each tagged release and
attached to the [GitHub
release](https://github.com/Vulpus-Labs/patches/releases) for that
version.

This page exists as a navigable entry point in the manual; the
content lives in the repo so the release tooling has a single
canonical source.
