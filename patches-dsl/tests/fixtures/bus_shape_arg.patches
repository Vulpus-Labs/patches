template Bus(size: int) {
    in:  x
    out: y
    module m : Sum(channels: <size>)
    m.in[0] <- $.x
    $.y     <- m.out
}
patch {
    module b   : Bus(size: 4)
    module out : AudioOut
    out.in_left[0] <-[1.0]- b.y
}
