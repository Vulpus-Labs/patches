pattern kick { trig: x . }

template t(s: song) {
    in: d out: d
    module seq : MasterSequencer([ch]) { song: <s> }
    module out : AudioOut
    seq.clock[ch] -> out.in
}

patch {
    module t : t(kick)
}
