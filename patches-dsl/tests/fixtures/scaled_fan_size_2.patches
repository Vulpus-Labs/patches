template ScaledFan(size: int) {
    in:  ch[size]
    out: mix
    module m : Sum(<size>)
    m.in[*size] <- $.ch[*size]
    $.mix <- m.out
}
patch {
    module osc : Osc
    module fan : ScaledFan(2)
    module out : AudioOut
    osc.sine[0] -[0.5]-> fan.ch[0]
    osc.sine[0] -[0.5]-> fan.ch[1]
    out.in[0] <-[1.0]- fan.mix
}
