pattern kick { trig: x . }

template t(pat: pattern) {
    in: d out: d
    song s(ch) {
        play { <pat> }
    }
    module seq : MasterSequencer([ch]) { song: s }
    module out : AudioOut
    seq.clock[ch] -> out.in
}

patch {
    module t : t(kick)
}
