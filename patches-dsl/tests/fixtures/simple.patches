# Baseline smoke-test: flat patch, no templates.
# Single mono->stereo broadcast cable into AudioOut.

patch {
    module osc : Osc { frequency: 440Hz }
    module out : AudioOut

    osc.sine -> out.in
}
