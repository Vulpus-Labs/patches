# Baseline smoke-test: flat patch, no templates.
# Connections using both -> and <-. No scaling, no indexed ports, no shape args.

patch {
    module osc : Osc { frequency: 440Hz }
    module out : AudioOut

    osc.sine  -> out.in_left
    out.in_right <- osc.sine
}
