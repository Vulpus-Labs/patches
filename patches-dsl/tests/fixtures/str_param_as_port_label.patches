template pick(port: str) {
    in:  inp
    out: out
    module osc : Osc
    osc.<port> -> $.out
    $.inp -> osc.voct
}
patch {
    module t : pick(sine)
    module out : AudioOut
    out.in <- t.out
}
