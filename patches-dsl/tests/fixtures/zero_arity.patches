template ZeroArity(n: int) {
    in:  ch[n]
    out: out
    module m : Sum(1)
    m.in[*n] <- $.ch[*n]
    $.out    <- m.out
}
patch {
    module t   : ZeroArity(0)
    module out : AudioOut
    out.in[0] <-[1.0]- t.out
}
