[package]
name = "example-wavefolder-wasm"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
patches-core = { path = "../../patches-core" }
patches-wasm-sdk = { path = "../../patches-wasm-sdk" }
