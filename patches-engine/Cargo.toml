[package]
name = "patches-engine"
version = "0.6.0"
edition = "2021"
publish = false

[dependencies]
patches-core = { path = "../patches-core" }
patches-planner = { path = "../patches-planner" }
patches-ffi-common = { path = "../patches-ffi-common" }
patches-dsp = { path = "../patches-dsp" }
patches-io-ring = { path = "../patches-io-ring" }
rtrb = "0.3"
midir = "0.10"

[dev-dependencies]
patches-core = { path = "../patches-core", features = ["test-support"] }
patches-modules = { path = "../patches-modules" }

[lib]
name = "patches_engine"
path = "src/lib.rs"
doctest = false
