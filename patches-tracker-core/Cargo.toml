[package]
name = "patches-tracker-core"
version = "0.6.0"
edition = "2021"
publish = false

[lib]
name = "patches_tracker_core"
path = "src/lib.rs"

[dependencies]
patches-core = { path = "../patches-core" }
