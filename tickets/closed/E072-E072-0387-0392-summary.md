# Epics E072–E072: tickets 0387–0392

## E072: Patch SVG rendering (LSP + CLI + VSC panel)

Expose the expanded patch graph as an SVG document from three entry points: a standalone CLI binary for scripted/doc usage, a custom LSP request consumed by the VS Code extension (side-panel webview), and the existing clap GUI (migrated to the shared layout).

Tickets: 0387, 0388, 0389, 0390, 0391, 0392
