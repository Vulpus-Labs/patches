[package]
name = "patches-ffi-common"
version = "0.1.0"
edition = "2021"

[lib]
name = "patches_ffi_common"
path = "src/lib.rs"

[[bin]]
name = "descriptor-hash-probe"
path = "src/bin/descriptor_hash_probe.rs"

[dependencies]
patches-core = { path = "../patches-core" }
rtrb = "0.3"
tracing = "0.1"
